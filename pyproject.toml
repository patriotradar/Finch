[project]
name = "finch"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.100.0",
  "uvicorn[standard]>=0.30.0",
  "pyyaml>=6.0",
  "websockets>=12.0",
]

[tool.vercel]
entrypoint = "app.py"
